apiVersion: v1
kind: Service
metadata:
  name: petclinic
  namespace: harness-petclinic
  labels:
    app: petclinic
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30081
      protocol: TCP
  selector:
    app: petclinic
